const numbers = [87, 54, 12, 63, 41];

for(const number of numbers ){
    console.log('current item is:', number);
}

const friends = ['jamir', 'khamir', 'namir', 'damir'];
for(const f of friends){
    console.log(f);
}